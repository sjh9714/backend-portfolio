# 확인이 필요한 문장

갱신: 2026-08-06 (로컬 클론 코드 대조 이후)

## 해소된 것

### 서술 구조 폐기로 자동 해소 (2026-08-05)

직전 버전은 훅→기→승→전→결의 서술체였고, 그 형식이 **동기·발견 경위·소감**을 요구했다.
자료가 규정한 구조(제목 / 그림 / 문제 원인 3 / 해결 과정 3~4 / 결과 3)로 바꾸면서
그 문장들이 들어갈 자리 자체가 없어졌다. 아래는 전부 삭제됨.

- "이 규칙을 외우는 대신 어느 조건에서 무엇이 무너지는지 직접 보고 싶었습니다" (동기)
- "검증받아야 할 대상이 아니라 이 도구가 먼저 버그를 잡았습니다" (발견 경위)
- "그래서 기능 목록부터 만들지 않았습니다" (작업 순서)
- "여기서 배웠습니다" 류의 소감 전부

### 코드 대조로 해소 (2026-08-06)

로컬 클론(`~/Projects/*`)의 실제 코드를 읽어 확인했다. **두 건은 틀렸고 수정했다.**

| 항목 | 결과 |
|---|---|
| 목록 조회 쿼리 수 | ❌ "101회 → 1회"는 **틀림**. 기능 추가로 IN 배치 2개가 붙어 현재 **3회 고정**. 전 문서에서 수정 |
| 캐시 무효화 | ❌ "전체 무효화 제거"는 **틀림**. 잦은 이벤트만 선택 무효화이고 방 생성·참여 3곳은 `allEntries = true`. 수정 |
| 프로젝션 6개 값 | ✅ 맞음 (`ChatRoomListResponse`는 필드 11개지만 프로젝션이 채우는 건 6개) |
| Redis Cache Aside TTL 5분 | ✅ 맞음 (`entryTtl(Duration.ofMinutes(5))`) |
| billing-gateway 버전 | ✅ 확인 완료 — Spring Boot 3.5.14 · Java 21 · postgres 16 · redis 7. 사이트에 반영 |
| eta 참여 인력 | ✅ README와 일치. 개발 4명의 FE/BE 내부 분담은 명시돼 있지 않아 추정하지 않음 |
| concert-booking 버전 | ✅ Spring Boot 3.4.1 · Java 21 · Kafka cp 7.6.0 · Redisson 3.40.2 |
| realtime-chat 버전 | ✅ Spring Boot 3.4.3 · Java 21 · Kafka 3.9.0 |

상세 근거는 각 `docs/facts/<slug>.md`의 "코드 대조" / "버전" 절에 있다.

---

## 남은 것

### 🔴 realtime-chat 재측정 — 사용자 작업

절차와 저장소 자체의 「근거 승격 체크리스트」는 `docs/facts/realtime-chat.md`에 있다.
Docker 데몬이 필요하다(로컬 클론 `~/Projects/realtime-chat`, k6 v1.5.0 설치 확인됨).

완료되면 `n-plus-one` 케이스의 **결과**에 실제 개선 수치를 추가할 수 있다.
단, 저장소 원칙상 `measured`가 아니라 `verified`로 두는 편이 맞을 수 있으니 artifact를 보고 판단할 것.

### 🟠 학력 · 자격증 — 사용자 입력 필요

확인된 사실이 없어 이력서에 싣지 않았다. 자료는 SI·금융권 지원 시 자격증을 우대하는
경향이 있다고 하므로, 있으면 `src/content/resume.ts`에 섹션을 추가하는 편이 좋다.
